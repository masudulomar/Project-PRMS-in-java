package prmsUtilities;

import java.io.*;/*
import javax.activation.*;
import javax.mail.Session;
import javax.mail.internet.*;
  */
public class ByteArrayTest {
  
	public static void main(String[] args) throws Exception {
		/*
		 * 
		 * final byte[] attachment = "Attachment".getBytes(); MimeMessage message = new
		 * MimeMessage((Session)null); MimeMultipart multipart = new MimeMultipart();
		 * MimeBodyPart part = new MimeBodyPart(); part.setDataHandler( new DataHandler(
		 * new DataSource() {
		 * 
		 * public String getContentType() { return "application/pdf"; }
		 * 
		 * public InputStream getInputStream() throws IOException { return new
		 * ByteArrayInputStream(attachment); }
		 * 
		 * public String getName() { return "myfile.pdf"; }
		 * 
		 * public OutputStream getOutputStream() throws IOException { return null; }}));
		 * 
		 * multipart.addBodyPart(part); message.setContent(multipart);
		 * message.writeTo(System.out);
		 */}
}